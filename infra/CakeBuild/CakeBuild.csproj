<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <RunWorkingDirectory>$(MSBuildProjectDirectory)</RunWorkingDirectory>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Cake.Frosting" />
    <PackageReference Include="Cake.Json" />
    <PackageReference Include="Newtonsoft.Json" />
    <!-- Pin transitive NuGet.* deps (from Cake.NuGet) to patched 7.3.1 - fixes NU1901 GHSA-g4vj-cjjj-v7hg -->
    <PackageReference Include="NuGet.Packaging" />
    <PackageReference Include="NuGet.Protocol" />
  </ItemGroup>

  <!-- Resolve the game install the same way the mod builds do: the VINTAGE_STORY env-var override if
       set, else the in-repo install provisioned into .game/1.22 (see scripts/provision-game.*). -->
  <PropertyGroup>
    <CakeGamePath Condition="'$(VINTAGE_STORY)' != ''"
      >$(VINTAGE_STORY)</CakeGamePath>
    <CakeGamePath Condition="'$(CakeGamePath)' == ''"
      >$([System.IO.Path]::GetFullPath('$(MSBuildThisFileDirectory)../../.game/1.22'))</CakeGamePath>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="VintagestoryAPI">
      <HintPath>$(CakeGamePath)/VintagestoryAPI.dll</HintPath>
    </Reference>
  </ItemGroup>
</Project>
