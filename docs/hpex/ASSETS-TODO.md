# hpex listing assets - still needed

`docs/hpex/` mirrors `docs/iiex/` but is missing the artwork, which can't be generated
from source. Until these land:

- `hpex_logo.jpg` / `hpex_logo_mini.jpg` - the ModDB listing logo (art by ariburin, same
  as the other mods).
- `screenshots/` - the ModDB gallery. `docs/iiex/screenshots/lancashire_boilers_smoke.png`
  and `cornish_engines.png` are already pictures of this mod's two machines and can move
  or be re-shot here.
- `src/HighPressureExpanded/modicon.png` is currently a **copy of iiex's icon** as a
  placeholder - replace it before publishing, or the two mods look identical in the mod
  manager.
