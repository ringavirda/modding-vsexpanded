# Work log - 2026-09

Running notes on uncommitted work, newest first. Each entry is roughly what a commit message would
have said. Past months live beside this file.

## 2026-09-05 - Per-mod layout

Moved everything a mod owns under one folder (`mods/<mod>/{src,tests,assets,docs,wiki}`), per
[2026-09-05-per-mod-layout.md](../plans/2026-09-05-per-mod-layout.md): `src/ExpandedLib/` ->
`mods/exlib/src/`, `test/IronIndustryExpanded.Tests/` -> `mods/iiex/tests/`, `assets/siex/` ->
`mods/siex/assets/siex/`, and so on for every mod, plus `assets/editable/` -> `workbench/` (shared
across mods), `dist/CakeBuild/` -> `infra/CakeBuild/`, `scripts/tools/` -> `infra/tools/`. `src/`,
`test/`, `assets/` and `docs/wiki` no longer exist; `dist/` holds only the gitignored `Releases/`.
Every `mv`, no `git mv` - the owner stages the renames.

**Build system.** `mods/Directory.Build.props`/`.targets` replace the old `src/`+`test/` pair: one
props for every project, test-only settings (runsettings, the linked `xunit.runner.json`, the legacy
`Using`) conditioned on the project name ending `.Tests` or `.Testing` (the harness needs the legacy
shim too, which the plan's literal `.Tests`-only condition would have missed). The targets pack a
mod's whole `assets/` tree unconditionally instead of one glob per domain, since a mod's tree can now
hold more than its own domain (iiex's `assets/game/` overlay); the old `AssetDomainsExtra`/
`ShipGameLangOverride` machinery is gone. `VintageStory.sln` regenerated with `exlib`/`iiex`/`siex`/
`infra` solution folders replacing `src`/`test`.

**Harness.** Added `RepoPaths` (`mods/exlib/testing/`) - `Root`, `Mod(id)`, `Assets(domain)` (maps
`game` to `iiex`), `Docs(modId)`, `AllAssetTrees()`. Every literal path in the harness and the test
suites now goes through it; `DefinitionGoldens.EmittedFamilies` scans every mod's asset tree instead
of one. `ReleasedVersions` (`mods/exlib/testing/`) replaces `ModinfoTests`' old scan of
`dist/Releases/` zip names - that folder is gitignored build output the packager clears on every run,
so it cannot hold release history; the highest-published-per-modid map is now a small hand-edited
table alongside the harness.

**Tooling and CI.** `infra/CakeBuild/Program.cs`'s `ModProject` carries the mod folder next to the
project name; every path it builds follows the table, and `../Releases` is now `../../dist/Releases`.
`.vscode/tasks.json`, the three GitHub workflows, `scripts/exmod.ps1` and `infra/tools/*.py`/`.cs`
updated to match. `convert-shape.py`/`generate-rolled-stock.py` read `workbench/shapes` instead of
`assets/editable/shapes`.

**Docs.** README, CONTRIBUTING, docs/internal/README and testing.md, the roadmap/walkthrough/
machining-line/STATE/NEXT plans, the per-mod READMEs and wikis, and the moddb/handbook pages'
screenshot links follow the mapping. `docs/internal/research/README.md` gets a short note mapping
old roots to new; its own snapshot citations keep the pre-move paths by rule, same as the closed
2026-08 plans, which were not rewritten.

**Gate**, in order: `dotnet build VintageStory.sln -clp:ErrorsOnly` and `-p:Legacy=true
-clp:ErrorsOnly` both green for the per-project/per-TFM form CI actually uses
(`dotnet test "<proj>" -p:Legacy=true -f <tfm>`); building the whole solution with `-p:Legacy=true`
in one invocation hits a pre-existing `CS1705` TFM-resolution race between multi-targeted project
references that reproduces identically on unmodified `HEAD` (verified via `git archive`) - CI
sidesteps it by building per project and TFM, which is why it was never seen before. `dotnet test
VintageStory.sln --nologo -v q` reproduces the pre-move baseline exactly: exlib 2019, iiex 2401, siex
324, all passing. `dotnet run --project infra/CakeBuild -- --target=Package` and `--target=
PackageTesting` both green; the three zips list `assets/<domain>/...` (iiex ships both `iiex` and
`game`). The tools and `scripts/exmod.sh` start clean. A path sweep over everything but
`docs/internal/research` and `docs/internal/worklog` finds only prose citations and the
packaged-layout `assets/<domain>` spelling.

Each mod's `README.md` and `CHANGELOG.md` sit at `mods/<mod>/`, beside its `src/`. Every code-path
citation in `docs/design` and `docs/internal/plans` follows the new layout (the research snapshots and
this log keep their original spellings); a link check over every markdown file finds only the
pre-existing `items/fettle.md` reference unresolved.

## 2026-09-05 - Casting bed drawn over its fillers

The bed's shape carried `AngleFromSide + 180` as well as its `StructureAngle`, so it drew its rows on the
side opposite the fillers that carve them, at every facing (north: draws z over [-0.5, 3.5] but reserves
[-3.5, 0.5]; south: [-3.5, 0.5] against [-0.5, 3.5]; east: draws x over [-3.5, 0.5] against [-0.5, 3.5];
west: draws x over [-0.5, 3.5] against [-3.5, 0.5]). The shape now spins by the side angle alone
(`ShapeSpunPerOrientation`, no offset) and `StructureAngle` keeps the +180; the blocktype golden was
re-blessed (`rotateYByType` only).

The rule: pair the shape's spin with `StructureAngle` only where the filler offsets are authored in the
shape's frame. Where the art grows one way and the footprint the other, the half turn goes on
`StructureAngle` alone, and then everything that follows the mesh turns by `Block.Shape.rotateY` while
everything that follows the footprint turns by `StructureAngle`. On the bed the mesh side is the per-cell
molten surfaces, which were spun by `StructureAngle` and are now spun by the shape's own angle.

The same half turn crosses the flanks over: a three-wide footprint turned 180 puts its west cells on the
east of the drawn bed, so `SandBedLayout` draws a west slot with `Mold{row}E` and an east slot with
`Mold{row}W`. Paired the other way the carve appeared across the spine from the cell that was clicked, at
all four facings. The whole-box comparison cannot see that, because the bed's footprint is symmetric
across the spine.

Guards: `CastingBedFootprintGuards` - `MegablockFrames.Misfit` null at all four sides, and every slot's
drawn element measured into the cell that carves it, in both the states that slot can hold, at all four
sides. Red before the fix with the extents above and with `A 'north' bed draws BedSlot { Row = 2, Side =
West } as Sand with 'Mold2WFull', centred (0.94, 1) blocks from the principal, but carves that slot at
cell (1, -1)`.

Walkthrough item A4 (runners and moulds read swapped east-west) is covered: the surface cuboid and the
HUD's mold count both tested `cellPos.X == Pos.X`, which picks out the spine only on a north- or
south-facing bed. Both now read the cell's slot, so a bed laid east or west gives its runners the channel
surface and counts its flanks.

## 2026-09-05 - Four walkthrough blockers fixed

Defect 1 (the helve route failed): the shingling work item was declared with vanilla's `ItemWorkItem`,
whose helve check answers NotWorkable to any recipe named neither plate nor blister steel. The item is now
its own registered `ItemShingleWorkItem` (code `shingleworkitem`, the metal as a variant), which answers
FullyWorkable; `IiexCollectibleClassTests` pins that every class string an iiex def names is registered,
and `PuddlingYieldTests` covers the item. Golden moved to `itemtypes/shingleworkitem.json`.

Defect 2 (casts reading as misruns): the misrun check ran at shake-out against the cell's live
temperature, by which time the metal had hardened far below any pour minimum, so every full cast was
scrapped. `BlockEntitySandCastingCell` now records the metal's temperature in the tick the cavity fills
(saved as `cc_filltemp`) and `CastingCellLogic.IsMisrun(cavityFull, pourTemp, minPourTemp)` judges that
value; a cell saved before the value existed shakes out clean. Tests: `CastingCellLogicTests`,
`CastingCellTests`.

Defect 3 (the long cell could not pour, and both cells drew their spout on the wrong face): the long
cell's shape carried the same +180 offset as its `StructureAngle`, so the body was drawn back onto the
footprint's own cell instead of opposite it - `The_drawn_mesh_lands_inside_the_reserved_footprint(side:
"north")` failed with `draws z over [-0.5, 1.5] but reserves [-1.5, 0.5] ... The shape spins by 180deg and
the footprint by 180deg`. Dropped the shape's offset so only `StructureAngle` carries the half turn.
Separately, `LaunderFace` read the block's raw facing rather than the face the launder spout is drawn on,
so the pull always looked at the filler cell (long cell) or the wrong side (both cells); `LaunderFace` now
turns the facing around. Guarded at every side, both cells, by `CastingCellFootprintGuards`, and pinned by
`The_long_cell_pulls_metal_fed_at_its_launder_face`. The long cell's blocktype golden was re-blessed
(`rotateYByType` only). The casting bed (`BlockSandCastingBed`) shows the identical fault at all four
sides (north: draws z over [-0.5, 3.5] but reserves [-3.5, 0.5]; south: draws z over [-3.5, 0.5] but
reserves [-0.5, 3.5]; east: draws x over [-3.5, 0.5] but reserves [-0.5, 3.5]; west: draws x over [-0.5,
3.5] but reserves [-3.5, 0.5]) - left unfixed, out of scope.

Defect 4 (a multi-lane pour yielded one piece): `PatternItemDefinitions.Mold` wrote its output stack with
no quantity, so the harvest's `Max(1, spec.Output.Quantity)` always fell back to one regardless of how
many lanes a pattern declared. `Mold` now carries an `outputQuantity` (3 for `castbillets`, 2 for
`castblooms`, 1 elsewhere) and the pattern golden was re-blessed. Tests:
`A_full_billet_pour_harvests_all_three_lanes`, `A_multi_lane_pattern_declares_a_lane_per_output`.

## 2026-09-04 (night) - Research persisted, walkthrough and machining-line plan written

Twelve read-only research agents (plus four sub-agent passes) mapped every iron-loop station and the
machining-line code surface; their reports are kept under `docs/internal/research/` (16 dated files,
indexed) so no session re-derives them. Deliverables: `docs/internal/plans/2026-09-04-phase1-walkthrough.md`
(per-station gestures, expected readouts, sixteen defects the code reading predicts - a probable crash on
the shingling work item's class, casts always reading as misruns, the long cell unable to pour, lids
named "Main door", beds never consumed while lit, among others); `docs/internal/plans/2026-09-04-machining-line.md`
(14 tasks: hold-to-operate + filler roles + cancel forward in exlib, the shared station window, tool
wear, one `machinetool` blocktype over eight stations, feed decision, interaction routing, drive-cell
spike, exports, cast blanks, intermediates and tools, job tables, the mill's raise/lower cells, recipes
and handbook, the gate); the U11 ladle unit carries a triage note with corrected paths. Roadmap and
NEXT.md point at both plans.

## 2026-09-04 (later) - Rulings recorded, line endings normalised, backlog written

The owner answered the roadmap's questions (Q0-Q5, Q7; Q6 open) - recorded in the roadmap's Rulings
table - and the roadmap gained the prioritised "What is left" list (items 1-37 across phases 0-5).
NEXT.md points at Phase 1. Later the same day the owner ruled the static ladle iiex and in the release (art
and layout exist; U11), and named the movable ladle and the skip hoist as one rail subsystem - recorded in
the roadmap, STATE.md, overview.md and ladle.md.

`cf2e8c89` committed the working tree as it arrived from Windows, so ~1,280 text files were stored with
CRLF. Done since: `* text=auto` added to `.gitattributes`; CR stripped from every tracked and untracked
text file (1,340 files; content unchanged - `git diff --ignore-cr-at-eol` shows only `.gitattributes`
and two deletions); the two OCR'd research texts at the root deleted (owner: not needed). The next
commit is the one-time renormalisation: `git add --renormalize .` and commit. Branches untouched
(`0.9-support` is the maintenance branch of the published old mods).

## 2026-09-04 - Re-assessment after the WSL move: roadmap written, planning layer reset

Repo verified under WSL: `dotnet build VintageStory.sln` clean, `dotnet test` on 1.22 = 2019 + 324 +
2368 = 4,711 passed (the gate the last Windows session recorded). `pwsh` resolves from
`.dotnet/tools`, so `scripts/exmod.sh` works.

- `docs/internal/plans/2026-09-04-roadmap.md` - the plan of record: state per loop (built / seen in
  game / craftable), assessment, phases 0-5, the parked list, open questions Q0-Q7 with recommendations.
- `NEXT.md` reduced to a one-page pointer; its 62 KB log survives at `git show 1060b7b0:docs/internal/plans/NEXT.md`.
- `STATE.md` refreshed: B15 struck (U9.9), B8 and B23 moved to the fixed list, B6 and B19 carry their
  new facts (rolled pressure-valve shape exists; B19's route is the machining line), the ladder rewritten
  to the built state with "walked" marked separately from "live", the closing section rewritten.
- `docs/internal/README.md` index corrected (five rows were stale; four files were missing).
- Superseded plans carry a status line: u2-u10 expansion closed 2026-08-22; iwex-bringup superseded by
  the roadmap; plan-audit and plan-coherence closed.
- Four design pages whose status lagged the code: puddling-furnace and reheat-furnace (shell -> live),
  processes/puddling (partial -> live), processes/shingling (designed -> partial: helve half built).
- Assistant memory: ~50 files stating overruled decisions deleted, three shrunk to current truth, index
  rewritten.

Not done, owner's call - the working tree arrived from Windows with 1,318 tracked files CRLF-flipped
(content identical to HEAD; only nine files differ in content). Restore with:

```
git diff --name-only > /tmp/all.txt
git diff --ignore-cr-at-eol --numstat | awk '($1>0||$2>0){print $3}' > /tmp/real.txt
grep -vxF -f /tmp/real.txt /tmp/all.txt | xargs -d '\n' git checkout --
for f in $(cat /tmp/real.txt) $(git status --short | grep '^??' | awk '{print $2}'); do sed -i 's/\r$//' "$f"; done
printf '# Normalise line endings on commit.\n* text=auto\n' | cat - .gitattributes > /tmp/ga && mv /tmp/ga .gitattributes
```

The 157 `git grep` hits for CR in HEAD are png/jpg/psd binaries and need nothing.
