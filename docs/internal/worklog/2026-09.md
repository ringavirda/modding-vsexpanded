# Work log — 2026-09

Running notes on uncommitted work, newest first. Each entry is roughly what a commit message would
have said. Past months live beside this file.

## 2026-09-04 - Re-assessment after the WSL move: roadmap written, planning layer reset

Repo verified under WSL: `dotnet build VintageStory.sln` clean, `dotnet test` on 1.22 = 2019 + 324 +
2368 = 4,711 passed (the gate the last Windows session recorded). `pwsh` resolves from
`.dotnet/tools`, so `scripts/exmod.sh` works.

- `docs/internal/plans/2026-09-04-roadmap.md` - the plan of record: state per loop (built / seen in
  game / craftable), assessment, phases 0-5, the parked list, open questions Q0-Q7 with recommendations.
- `NEXT.md` reduced to a one-page pointer; its 62 KB log survives at `git show 1060b7b0:docs/internal/plans/NEXT.md`.
- `STATE.md` refreshed: B15 struck (U9.9), B8 and B23 moved to the fixed list, B6 and B19 carry their
  new facts (rolled pressure-valve shape exists; B19's route is the machining line), the ladder rewritten
  to the built state with "walked" marked separately from "live", the closing section rewritten.
- `docs/internal/README.md` index corrected (five rows were stale; four files were missing).
- Superseded plans carry a status line: u2-u10 expansion closed 2026-08-22; iwex-bringup superseded by
  the roadmap; plan-audit and plan-coherence closed.
- Four design pages whose status lagged the code: puddling-furnace and reheat-furnace (shell -> live),
  processes/puddling (partial -> live), processes/shingling (designed -> partial: helve half built).
- Assistant memory: ~50 files stating overruled decisions deleted, three shrunk to current truth, index
  rewritten.

Not done, owner's call - the working tree arrived from Windows with 1,318 tracked files CRLF-flipped
(content identical to HEAD; only nine files differ in content). Restore with:

```
git diff --name-only > /tmp/all.txt
git diff --ignore-cr-at-eol --numstat | awk '($1>0||$2>0){print $3}' > /tmp/real.txt
grep -vxF -f /tmp/real.txt /tmp/all.txt | xargs -d '\n' git checkout --
for f in $(cat /tmp/real.txt) $(git status --short | grep '^??' | awk '{print $2}'); do sed -i 's/\r$//' "$f"; done
printf '# Normalise line endings on commit.\n* text=auto\n' | cat - .gitattributes > /tmp/ga && mv /tmp/ga .gitattributes
```

The 157 `git grep` hits for CR in HEAD are png/jpg/psd binaries and need nothing.
