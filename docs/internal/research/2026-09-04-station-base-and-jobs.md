# Research snapshot - station-base-and-jobs

**Written** 2026-09-04 by a read-only research agent, against commit `cf2e8c89` (the tree before the
line-ending renormalisation). **Covers** BlockEntityMachineStation, shear, fastener bench, ProcessJob registry, MachineTool, ItemDie, hold-to-operate precedents, station tests.
**Purpose** grounding for the roadmap's Phase 1 walkthrough and Phase 2 machining-line plan
(`docs/internal/plans/2026-09-04-*.md`).

Line numbers were right on that day and drift; cite by symbol when reusing. "UNVERIFIED" marks are the
agent's own and were not re-checked. Nothing here is a decision - decisions live on the design pages.

---

## Report: current code for the `machine-tool` family plan

### 1. `BlockEntityMachineStation` (exlib)

`src/ExpandedLib/Blocks/Machines/BlockEntityMachineStation.cs:19` - `public abstract class BlockEntityMachineStation : BlockEntityContainer`. Docstring (:8-18):

> "Base block entity for a machine the player works through a window: a container whose slots are declared as `MachineSlotSpec`s, plus the open/close handshake that keeps the server inventory in step with what the player sees. `BlockEntityContainer` routes none of the window packets itself, so every machine that opens a dialog re-implements the same handshake, the same claim check and the same dialog disposal. This carries all three once. A machine adds its own actions by overriding `OnStationPacket`, whose ids start at `FirstMachinePacketId`."

Surface:
- `public const int FirstMachinePacketId = 1002` (:27); 1000/1001 are open/close (:22-23).
- `protected abstract MachineSlotSpec[] SlotSpecs { get; }` (:33); `public override InventoryBase Inventory => _inventory ??= new MachineStationInventory(SlotSpecs)` (:35-36).
- `protected virtual GuiDialogBlockEntity? CreateDialog(ICoreClientAPI capi) => null` (:50-51) - null = windowless station.
- `protected bool WindowOpen` (:42); `public void ToggleWindow(IPlayer)` (:62-65, client-only, sends `Inventory.Open` + open packet :85-86); `protected virtual void CloseWindow()` (:91-96), called from `OnBlockRemoved`/`OnBlockUnloaded` (:98-106).
- `public override void Initialize(ICoreAPI api)` -> `Inventory.LateInitialize($"{InventoryClassName}-{Pos}", api)` (:53-56).
- `public sealed override void OnReceivedClientPacket(IPlayer, int, byte[])` (:117-155): close, `MayUse` claim/range check (:183-212), slot moves via `Inventory.InvNetworkUtil.HandleClientPacket` + chunk `MarkModified`, open, then `protected virtual bool OnStationPacket(IPlayer player, int packetid, byte[] data) => false` (:162-166). `internal virtual bool ValidatePickRange => true` (:176) for headless tests.
- Slots: `MachineStationSlots.cs:13-31` `readonly record struct MachineSlotSpec(Func<ItemStack?,bool>? Accepts, bool TakeOnly, string? HexBackgroundColor)` with `Input(accepts, hexColor)`, `AnyInput()`, `Output()`; `MachineStationInventory : InventoryGeneric` (:38-62); `ItemSlotMachineInput` (:66), `ItemSlotMachineOutput` (:82).
- No tool slot, no job selection, no progress: subclass concerns.

Subclasses: `BlockEntityWorkbench` (`Crafting/BlockEntities/BlockEntityWorkbench.cs:22`, windowed: `CreateDialog` :66-72, `PacketIdCraft = FirstMachinePacketId` :42, `OnStationPacket` :78-88), `BlockEntityDesignTable` (:23; `SlotSpecs` two inputs + `Output()` :36-41, `CreateDialog` :54-61, `PacketIdDraft` :34, `OnStationPacket` :64-75), `BlockEntityRollingMill` (`Forming/BlockEntities/BlockEntityRollingMill.cs:27`, windowless), `BlockEntityMpBench` (`Forming/BlockEntities/BlockEntityMpBench.cs:21-24`) -> `BlockEntityShear` (:27) and `BlockEntityFastenerBench` (:23), both windowless.

### 2. Shear

**`BlockShear`** (`src/IronIndustryExpanded/BlockStructures/Forming/Blocks/BlockShear.cs`): `[BlockRegister] public partial class BlockShear : BlockNetworkNode, IExBlockDefProvider, IFillerHost, IFillerInteractionTarget` (:29-34); `NetworkType => "mpenergy"` (:35).
- `Definitions(string domain)` (:42-64): `ExBlockDef.Create(domain, "forming", "forming/shear")`, `.VariantGroup("type", "shear")` (:53), `.VariantGroup("orientation", "ns", "we")`, `.NetworkOriented()`, `.ShapeByType("*-ns", "iiex:forming/shear", rotateY: 0)` / `"*-we"` 90 (:57-58), `.FillerOffsets(Footprint)` (:60), `.EntityBehavior("Animatable")`, `.SolidNonOpaque()`, `.Handbook("forming-shear-*")`.
- `Footprint` (:75-88): `StructureFootprint.Layout(f => f.Solid('I').Slab('_', BlockFacing.DOWN).Origin(-1, 1).Face(0, "_ _ _ / I O #"))`. `NestOffset = (-1,0,0)` (:92); `StructureAngle` (:99); `NestCell(BlockPos)` (:106-112); placement/removal via `StructureFillers` (:114-146).
- Interaction: `OnFillerInteractStart` (:161-166) and `OnBlockInteractStart` (:168-174) -> `HandleInteract(world, byPlayer, sel, clickedCell)` (:176-201), in order: wrench && `IsStroking` -> `FreeStuckPiece`; `MachineTool.IsTool(held)` -> `FitBlades` (any cell); empty hand -> `ShiftKey && TakeBlades`; else `clickedCell.Equals(NestCell(sel.Position)) && Crop(...)`. `FitBlades` (:220-245): `slot.TakeOut(1)` -> `shear.TryFitBlades(offered, out previous)`, refused -> returned to slot. `Crop` (:262-282): `shear.TryCrop(held)`; refusal -> `Report`; accepted -> `ActiveHotbarSlot.TakeOut(1)`.
- `OnFillerInteractStep => false` (:285-291); `OnFillerInteractStop` no-op (:294-300) - single click.
- `Report(IPlayer, ShearVerdict)` (:341-351) -> `SendIngameError` codes `iiex-shear-nobladeset|nojob|spent|bladetoosoft|notturning|notenoughdrive`; lang `assets/iiex/lang/en.json:123-128` (`game:ingameerror-iiex-shear-*`), help keys :117-119.

**`BlockEntityShear`** (`.../Forming/BlockEntities/BlockEntityShear.cs:27`, `: BlockEntityMpBench`):
- `BladeSlot = 0`, `PieceSlot = 1` (:31,:34); `InventoryClassName => "shear"`; `SlotSpecs => [MachineSlotSpec.Input(MachineTool.IsTool, "#6A5A3A"), MachineSlotSpec.AnyInput()]` (:38-42).
- `public const string MachineKey = "shear"` (:76); `Jobs { get; protected set; } = ProcessJobRegistry.Shared` (:72-73).
- `BladeTier => MachineTool.TierOf(_blades)` (:64); `HasBlades => BladeTier >= 0` (:67).
- `ProcessJob? JobFor(ItemStack?)` (:82-94): `Jobs.Job(MachineKey, code.ToShortString(), piece?.Thickness, piece?.Family)` with `WorkPiece.FromStack`.
- `ShearDecision TryCrop(ItemStack?)` (:101-132): `ShearFeed.Decide(HasBlades, BladeTier, JobFor(stack), WorkPiece.FromStack(stack), tempC, IiexValues.RollingTempC, IiexValues.ShearColdMultiplier, AvailableTorque, Speed)`; on accept clones one piece, stores `_tempC`, `BeginStroke(decision.Job!.Seconds)`.
- `CompleteStroke()` (:136-153): re-resolves job; `Eject(Resolve(job.Output, job.Stage == null ? job.Count : 1)); Eject(Remainder(input, job))`; `Remainder` (:159-167) tallies `piece.Crop(job.Count)`.
- `ReleaseStuckPiece()` (:173-181); `TryFitBlades(ItemStack? blades, out ItemStack? previous)` (:191-202, refused mid-stroke).
- `LoadTorque(float speed)` (:213-221): `ShearFeed.RequiredTorque(job, _tempC, ...)` while stroking, else 0.
- Persists `shearTempC` (:245-258); `OnBlockBroken` ejects blades and piece (:262-269).

**`ShearFeed.cs`**: `enum ShearVerdict { Ok, NoBladeSet, NoJob, Spent, BladeTooSoft, NotTurning, NotEnoughDrive }` (:7-32); `readonly record struct ShearDecision(ShearVerdict Verdict, ProcessJob? Job, float RequiredTorque)` + `Accepted` (:39-45); `RequiredTorque(ProcessJob job, float tempC, float rollingTempC, float coldMultiplier)` (:62-70, `MinTorque` x cold multiplier); `Decide(bool hasBladeSet, int bladeTier, ProcessJob? job, WorkPiece? piece, float tempC, float rollingTempC, float coldMultiplier, float availableTorque, float speed)` (:82-116): reads `job.Stage`, `piece.IsSpent(job.Count)` (:104), `bladeTier < job.MinTier` (:107), `RollingPass.CanCarry(required, availableTorque, speed)` (:113; `RollingPass.cs:100-104` = `speed > 0f && availableTorque >= loadTorque`).

**`BlockEntityMpBench`** (shared base, iiex-local): `abstract class BlockEntityMpBench : BlockEntityMachineStation, IMpEnergyConsumer, IProductionReadiness` (:21-24). Ctor adds `HostMembership : BEBehaviorNetworkMember` (`NetworkType "mpenergy"`, :44-50) and `HostProcess : BEBehaviorProductionMachine` with `ProductionTickMs => 250` -> `owner.AdvanceStroke(dt)` (:54-66). `IsReadyToProduce => IsStroking` (:71), `StopsProductionWhenNotReady => false` (:75). Stroke clock: `IsStroking`, `Remaining` (:85-88), `protected void BeginStroke(float seconds)` (:92), `AbandonStroke()` (:96), `public bool AdvanceStroke(float dt)` (:104-116) - advances only while `Speed > 0`, calls `protected abstract void CompleteStroke()` (:119). `Resolve(string code, int quantity)` (:129-145), `Eject(ItemStack?)` (:149-153, spawns at Pos+(0.5,1,0.5)). Network reads: `Speed => RunState?.Speed` (:163), `AvailableTorque => run.SupplyPower / run.Speed` (:172-173), `public abstract float LoadTorque(float speed)` (:177). Persists `benchRemaining` (legacy `shearRemaining`, :183-201).

**mpenergy contract**: `IMpEnergyConsumer` has exactly one member, `float LoadTorque(float speed)` (`src/ExpandedLib/Networks/MpEnergyNodes.cs:33-38`). There is no `RequestedEnergy`/`OnEnergyGranted` anywhere in exlib (grep); the run pulls `LoadTorque` and the machine reads back `Speed`/`SupplyPower`.

**Drive connector**: the principal's connectors are the `orientation` variant letters - `BlockNetworkNode.HasConnectorAt(BlockFacing face) => Orientation != null && Orientation.Contains(face.Code[0])` (`BlockNetworkNode.cs:754-755`), `GetConnectorFaces()` (:774-777), `Orientation` set in `OnLoaded` (:37-41); `NetworkOriented()` attaches `ExOrientable` in `network` mode (`ExBlockDef.cs:498-513`). Neither shear nor benches use `BEBehaviorMPFillerPort` (files referencing it: flywheel, twin-tub blower, structure infrastructure only). `BEBehaviorMPFillerPort : BEBehaviorMPBase, IFillerHostedBehavior` (`BEBehaviorMPFillerPort.cs:21-23`) is a vanilla-MP intake, hosted by `f.Host('M', FillerBehaviorSpec.Of<BEBehaviorMPFillerPort>("north"), ...)` (`BlockFlywheel.cs:36-52`; `FillerLayoutBuilder.Host` :69-76). Design: nail-machine.md:48 and rivet-machine.md:49-52 say the connector sits on the principal because no mpenergy filler-cell connector ships, "all three want re-homing together when the station family lands"; machines.txt:6-7 puts the drive on `M`/`m` cells for every machine except the cutter (:52). A passive `FillerLayoutBuilder.Port(char symbol, BlockFacing face, string networkType)` (:104-114) answers `HasConnectorAt` for the principal without joining the graph - UNVERIFIED whether `MpEnergyNetwork` discovery honours it.

### 3. Fastener bench

`BlockFastenerBench` (`.../Forming/Blocks/BlockFastenerBench.cs:32-37`, same interfaces as the shear). `NailCutter = "nailcutter"`, `Riveter = "riveter"` (:43,:46). `Definitions` (:53-88): `ExBlockDef.Create(domain, "forming", "forming/bench")`, `.VariantGroup("type", NailCutter, Riveter)` (:67), `.ShapeByType($"*-{NailCutter}-ns", "iiex:forming/nailcutter", rotateY: 0)` x4 (:70-81), `.FillerOffsetsByType($"*-{NailCutter}-*", NailCutterFootprint)` / Riveter (:82-83). Footprints: nail cutter `f.Solid('I').Solid('m').Origin(0, 1).Slice(0, "I m / O #")` (:104-116); riveter `f.Solid('I').Solid('M').Origin(-1, 1).Face(0, "# M # / I O #")` (:126-138). `Faces` dictionary `[NailCutter]=(0,1,0)`, `[Riveter]=(-1,0,0)` (:142-145); `MachineKey => Variant?["type"] ?? NailCutter` (:155); `FaceCell(BlockPos)` (:162-168). `HandleInteract` (:233-258) mirrors the shear with `ItemDie.IsDie(held)` -> `FitDie`, `FaceCell` -> `Press`; `OnFillerInteractStep => false` (:341-347); `Report` codes `iiex-bench-nodie|nojob|notturning|notenoughdrive` (:397-405; en.json:140-143). Help strings via `IiexLang.Bench*` constants generated by `ExLangKeyGenerator` (`src/ExpandedLib.Generators/ExLangKeyGenerator.cs`; csproj :19).

`BlockEntityFastenerBench` (`.../BlockEntities/BlockEntityFastenerBench.cs:23`): `DieSlot = 0`, `BlankSlot = 1`; `SlotSpecs => [Input(ItemDie.IsDie, "#6A5A3A"), AnyInput()]` (:34-38); `HasDie` (:51); `MachineKey => Block?.Variant?["type"] ?? ""` (:62); `JobFor` (:68-94) parses `_die.Collectible.Attributes[ItemDie.AttributeKey]` with `ItemDie.TryParse`, refuses `set.Machine != MachineKey` (OrdinalIgnoreCase, :81-88), then `job.Matches(code.ToShortString(), null, null)`; `TryPress` (:101-119) -> `BenchFeed.Decide(HasDie, JobFor(stack), AvailableTorque, Speed)` -> `BeginStroke(job.Seconds)`; `CompleteStroke` (:126-141) `Eject(Resolve(job.Output, job.Count))`; `TryFitDie` (:165-176); `LoadTorque => job.MinTorque` while stroking (:185-186). `BenchFeed.cs`: `enum BenchVerdict { Ok, NoDie, NoJob, NotTurning, NotEnoughDrive }` (:6-24), `BenchDecision(BenchVerdict Verdict, ProcessJob? Job)` (:29-34), `Decide(bool hasDie, ProcessJob? job, float availableTorque, float speed)` (:54-69).

Dies: `BenchDieItemDefinitions.cs:19-70` - `NailMachine = "nailcutter"`, `RivetMachine = "riveter"`, `NailsPerPlate = 4`; `ItemDie.Itemtype(domain, { ["nail"] = ItemDie.Job(NailMachine, $"{domain}:nailplate", "game:metalnailsandstrips-iron", count: 4, minTorque: 0.2, seconds: 2), ["rivet"] = ItemDie.Job(RivetMachine, $"{domain}:rivetrod", ..., minTorque: 0.3, seconds: 3) }, "game:item/ingot")` -> `iiex:die-nail`, `iiex:die-rivet` (en.json:131-132). No hold flow exists.

### 4. Jobs

`ProcessJob.cs:22-31`: `public sealed record ProcessJob(string Input, string Output, int Count, float? Stage, string? Family, float MinTorque, int MinTier = 0, float Seconds = ProcessJob.DefaultSeconds)`; `DefaultSeconds = 1f` (:34); `bool Matches(string input, float? stage, string? family)` (:39-47). `ProcessJobSet(int Schema, string Machine, ProcessJob[] Jobs)` (:57-61), `CurrentSchema = SpecSchema.First` (:63), `static bool TryParse(JsonObject? node, out ProcessJobSet? set, out string? error)` (:70-134) - fields `schema`, `machine`, `jobs[].{input, output, count>=1 (default 1), stage (>0 else null), family, minTorque (0), minTier (0), seconds (>0 else 1)}`.

`ProcessJobRegistry.cs`: `Shared` (:16); `IReadOnlyList<string> Contribute(ProcessJobSet set)` (:26-46) - same `Matches(Input, Stage, Family)` with different Output/Count is reported, first stands, identical is silent; `IReadOnlyList<ProcessJob> Jobs(string? machine)` (:49-53); `ProcessJob? Job(string? machine, string? input, float? stage, string? family)` (:61-76, staged preferred); `Machines` (:79); `Clear()` (:82).

`ProcessJobLoader.cs`: `CataloguePath = "config/processjobs/"` (:17); `Parse(IEnumerable<(string Source, string Json)>, out List<string> errors)` (:24-53); `Load(files, ProcessJobRegistry? registry = null)` (:60-79) clears then contributes; `Read(ICoreAPI)` uses `api.Assets.GetMany(CataloguePath)` (:82-87); `Load(ICoreAPI api)` (:91) called from `ExpandedLibModSystem.cs:67-68`.

Files (all that exist): `assets/iiex/config/processjobs/shear.json` (5 jobs) and `assets/siex/config/processjobs/shear.json` (4 jobs, all `minTier: 2`). Example (iiex :5-13): `{ "input": "iiex:stock-shingledbar", "stage": 2.0, "family": "grooved", "output": "game:rod-iron", "count": 4, "minTorque": 0.2, "seconds": 2 }`. Guard pattern: `ShippedCropTableTests.cs:22-40` parses the shipped file through `ProcessJobSet.TryParse`.

`SpecSchema.cs`: `Key = "schema"` (:14), `First = 1` (:19), `TryRead(JsonObject? node, int current, out int schema, out string? error)` (:26-46): absent -> 1, newer -> refused.

### 5. `MachineTool`

`MachineTool.cs`: `AttributeKey = "machinetool"` (:26); `TryParse(JsonObject? node, out int tier, out string? error)` (:35-53, reads `tier` >= 0 after `SpecSchema.TryRead`); `IsTool(ItemStack?)` (:56-57); `TierOf(ItemStack?)` -> -1 when not tooling (:64-67); `Tier(int)` (:71-72); `Itemtype(string domain, string code, IReadOnlyDictionary<string,int> tiers, string shape = "game:item/ingot", string variantGroup = "type")` (:85-104, `MaxStackSize(1)`, `attributesByType["*-"+type]`). Only implementer: `ShearBladeItemDefinitions.cs:19-35` - `iiex:shearblade-iron` (tier 1), `iiex:shearblade-steel` (tier 2), variant group `metal`. Tier compare: `ShearFeed.Decide` :107-108. **Wear is not applied anywhere** - grep for `DamageItem`/`Durability` in Forming and Processes hits only a comment (`ItemDie.cs:122`); tooling-wear.md:3 "nothing built".

### 6. `ItemDie`; `MachineJob`

`MachineJob` does not exist as a type; machining-line.md:249 records that it "turned out to be `ProcessJob`". `ItemDie.cs`: `AttributeKey = "machinejob"` (:20); `TryParse(JsonObject?, out ProcessJobSet?, out string?)` -> `ProcessJobSet.TryParse` (:23-34); `IsDie` (:37-38); `JobFor(ItemStack? stack, string? input, float? stage, string? family)` (:45-65) - does **not** check machine (the bench does); `Job(string machine, string input, string output, int count = 1, int minTier = 0, double minTorque = 0, double seconds = ProcessJob.DefaultSeconds)` (:71-97) emits `{ machinejob: { schema, machine, jobs: [...] } }`; `Itemtype(string domain, IReadOnlyDictionary<string, object> jobs, string shape = "game:item/ingot", IReadOnlyDictionary<string,string>? shapeByType = null)` (:108-134) -> itemtype `die`, `VariantGroup("type", ...)`. A die names its machine through `ProcessJobSet.Machine`.

### 7. Hold-to-operate

Engine idiom: `OnBlockInteractStart` returns true -> `OnBlockInteractStep(float secondsUsed, ...)` fires each tick while RMB is held, false ends it; `OnBlockInteractStop`/`Cancel` on release. Vanilla: `BlockQuern.cs:37-86` (start `SetPlayerGrinding(byPlayer, true)`, step `beQuern.IsGrinding(byPlayer); return beQuern.CanGrind()`); `BEQuern.cs` keeps `playersGrinding` UID->ms (:62, :357-369), `Every100ms` advances `inputGrindTime += dt * grindSpeed` only when `quantityPlayersGrinding > 0` (:77, :285-292), and evicts holders silent for 1000 ms (:342-349). Also `BlockGrindingWheel.cs:30-44`, `BlockFruitPress.cs:127-148`.

Mod: `BlockManualFluidPump.cs:158-215` - `OnBlockInteractStep` -> `be.OnPumpStep(); return true` while hand empty, `OnBlockInteractStop` -> `be.OnPumpStop()`, and the same via `IFillerInteractionTarget.OnFillerInteractStep/Stop`. `BlockConverterControl.cs:131-178` (siex) - timed hold: start returns true (:144), step returns true until `secondsUsed >= SiexValues.BessemerPourHoldSeconds`, then commits (:171-176). Filler routing exists: `BlockStructureFiller.OnBlockInteractStep` (:261-279) repoints to the principal's `IFillerInteractionTarget.OnFillerInteractStep(secondsUsed, world, byPlayer, psel, clickedCell)` (interface `IFillerInteractionTarget.cs:16-45`). No station uses it: both return false (section 2, section 3), and `AdvanceStroke` runs on the 250 ms production clock whenever the run turns.

Workbench Craft button (`GuiDialogWorkbench.cs`): `RepeatSeconds = 0.35f` (:25); `OnMouseDown` arms `_holdingCraft` (:194-204), `OnMouseUp` clears (:206-209), `OnRenderGUI(float deltaTime)` sends `SendCraft()` = `_capi.Network.SendBlockEntityPacket(_pos, BlockEntityWorkbench.PacketIdCraft)` every interval (:188-192, :211-222); server `BlockEntityWorkbench.OnStationPacket` -> `TryCraft(player)` (:78-88). A client packet loop, not a held world interaction. The workbench "interaction sequence" (`config/craftsequences/workbench.json`, `seconds` = "How long RMB is held") is design only (workbench.md:3, :75-106).

### 8. Tests

`test/IronIndustryExpanded.Tests/Blocks/Forming/ShearStationTests.cs` (358 lines): fixture `Shear(ProcessJobRegistry? jobs = null)` (:26-47): `new TestWorld()`, `world.RegisterNetwork("mpenergy", n => new MpEnergyNetwork(n))`, `RegisterItem`, `TestBlocks.Configure(new BlockShear(), "iiex:forming-shear-ns", 1, ("type","shear"), ("orientation","ns"))`, `world.Place(pos, block, shear)`, `world.Initialize(shear)`. `TestShear : BlockEntityShear` injects `Jobs` (:50-55); `Registry(count, minTier, minTorque)` contributes a `ProcessJobSet(CurrentSchema, BlockEntityShear.MachineKey, [...])` (:57-84); `Stock(world, cropped)` writes a `WorkPiece(...).ToStack` (:86-97); `Blades(world, tier)` sets `{ "machinetool": { "tier": N } }` (:99-107); `Turning(world, pos, speed=1, supplyPower=100)` -> `((MpEnergyNetwork)world.NetworkAt(pos)).RestoreState(new MpEnergyNetworkState{...})` (:331-345), `Stopped` (:347), `Advance` steps `AdvanceStroke(0.25f)` (:352-355). Representative `A_crop_yields_one_product_and_the_remainder_with_one_more_tallied` (:193-219): `Shear(Registry())` -> `TryFitBlades(Blades(world,1))` -> `Turning` -> `TryCrop(Stock(world))` -> `Advance(1.5f)` -> assert `world.Drops` = product + remainder with `WorkPiece.Cropped == 1`.

`FastenerBenchTests.cs` (423 lines): `Bench(string type = BlockFastenerBench.NailCutter)` (:36-56) same pattern with `$"iiex:forming-{type}-ns"`; `Die(...)` builds attributes from `JToken.FromObject(ItemDie.Job(...))` (:62-85); representative `A_nail_plate_becomes_its_bundles_and_nothing_comes_back` (:292-309), `The_bench_reads_its_machine_key_off_its_own_variant` Theory (:190-196), `Every_shipped_die_names_a_bench_that_exists` (:397-421). Harness: `TestWorld` (`test/ExpandedLib.Testing/TestWorld.cs:25`; `Drops` :76, `Initialize(BlockEntity)` :110, `RegisterNetwork` :119, `Place` :133, `NetworkAt` :464), `TestBlocks.Configure<T>(T block, string code, int id, params (string key, string value)[] variants) where T : Block` (`TestBlocks.cs:19-25`). No StructureRig; the footprint test reads the def JSON via `StructureFillers.ReadOffsets` (:110-138).

## What is missing for the family

1. **Hold-to-operate.** Nothing consumes `ProcessJob.Seconds` only while held. Block side: replace `OnFillerInteractStep => false` (`BlockShear.cs:285`, `BlockFastenerBench.cs:341`) with the pump's pattern (`BlockManualFluidPump.cs:158-215`); BE side: a held-flag with the quern's timestamp eviction, gating `AdvanceStroke` (`BlockEntityMpBench.cs:104-116`, currently iiex-local; roadmap :180-181 wants it in exlib, shared with the workbench sequence).
2. **Shared window.** Both benches inherit `CreateDialog => null` (`BlockEntityMachineStation.cs:50-51`); the only dialogs are iiex-local `GuiDialogWorkbench`/`GuiDialogDesignTable`. Goes in `src/ExpandedLib/Blocks/Machines/` extracted from `GuiDialogDesignTable.cs` (roadmap :183-184). Open with the owner whether the shear adopts it (machining-line.md:379-381).
3. **Seven footprints.** Only shear, nail cutter, riveter exist. machines.txt: drill press :73-77, bore :95-101, lathe :119-125, planer :157-169, shaper :229-232; slab legend is redefined per machine, so each needs its own `Slab(char, BlockFacing)`. Builder verbs available: `Origin`, `Solid`, `Attach`, `Host`, `Slab`, `Port`, `Layer(int y, ...)`, `Slice(int x, ...)`, `Face(int z, ...)` (`FillerLayoutBuilder.cs:37-133`); declare with `FillerOffsetsByType` (`ExBlockDef.cs:801-815`).
4. **Seven exports.** Runtime `assets/iiex/shapes/forming/` ships `shear.json`, `nailcutter.json`, `riveter.json`; editable `assets/editable/shapes/machines/mpenergy/` has cutter, drillpress, horizontalboring, lathe, nailcutter, planer, riveter, shaper. Missing: drillpress, horizontalboring, lathe, planer, shaper, plus `ShapeByType` rows per type.
5. **Job tables.** Only `config/processjobs/shear.json` (iiex, siex). Lathe/bore/shaper/planer/drill files go beside it; the loader needs no change (`ProcessJobLoader.cs:82-87`); copy `ShippedCropTableTests` as the guard.
6. **`MachineKey` from variant.** `BlockEntityShear.MachineKey` is `const "shear"` (`BlockEntityShear.cs:76`; machining-line.md:370 misplaces it on `BlockShear`); `BlockEntityFastenerBench.MachineKey => Block?.Variant?["type"]` (:62) is the target. Folding also means one BE and one decision type over two axes the code keeps apart today: tool-vs-die (`MachineTool`/`ProcessJobRegistry.Job` vs `ItemDie`/`ProcessJobSet.Machine`) and staged-crop vs whole-item (`CompleteStroke` in each BE).

Additional gaps worth naming: no mpenergy filler-cell connector (design wants the drive on `M`/`m`; `Port()` is a candidate, UNVERIFIED for mpenergy); tool wear unbuilt; `IMpEnergyConsumer` has no request/grant handshake to design against; shear.md:283-284 still describes the pre-build class shape ("minus `IFillerHost`", "`BlockEntityNetworkNode`").
